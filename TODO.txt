*) determine cause of gpart crash in FreeBSD installer

*) build and test on platforms other than amd64

*) consider integrating decompression? FreeBSD-15.0-RELEASE-amd64-bootonly.iso
is 4.5x the size of FreeBSD-15.0-RELEASE-amd64-bootonly.iso.xz ... other
isos don't seem to have as significant of a compression ratio though

*) reformat code

*) build instructions

